# DESIGN.md — UI/UX 디자인

> **문서 상태:** 초안 v0.1 (승인 대기)
> **스택:** HTML + TailwindCSS (반응형). 데이터는 `SPEC.md`의 JSON 스키마와 1:1 바인딩.
> **두 개의 UI:** (A) 관리자용 백오피스, (B) 고객용 프론트엔드 대시보드.

---

## 0. 디자인 원칙

| # | 원칙 | 설명 |
|---|------|------|
| D1 | **수치는 확정값만 표시** | 화면의 모든 숫자는 `financials.*` / `dashboard_payload`의 확정값을 그대로 렌더. 프론트에서 재계산하지 않는다(Strict Rule의 UI 연장). |
| D2 | **원본 대비 분석 병렬 표시** | 백오피스는 항상 "확정 수치(원본)"와 "에이전트 서술"을 나란히 보여 검증 가능하게 한다. |
| D3 | **상태 가시성** | 모든 리포트는 상태 배지(초안/검토중/재작성중/승인/발행)를 노출. |
| D4 | **모바일 우선(고객)** | 고객 대시보드는 사장님이 휴대폰으로 보는 것을 전제로 반응형·카드 중심. |
| D5 | **일관 토큰** | 색/여백/타이포는 Tailwind 유틸리티 토큰으로 통일. 라이트/다크 모두 고려. |

**상태 배지 색상 규약(제안):** `computed`=slate, `drafting`=blue, `review_pending`=amber, `revising`=violet, `approved`=green, `published`=emerald, `failed`=red.

---

## A. 관리자용 백오피스

관리자(나 + 파트너)가 데이터를 올리고, 에이전트 초안을 검토하고, 피드백을 보내고,
재작성 결과를 비교해 최종 승인·발행하는 내부 도구.

### A-0. 전역 레이아웃

```
┌───────────────────────────────────────────────────────────┐
│  [로고] 재무컨설팅 백오피스            [사용자]  [로그아웃]  │
├───────────┬───────────────────────────────────────────────┤
│ 사이드바   │  메인 컨텐츠 영역                              │
│ · 대시보드 │                                               │
│ · 데이터   │                                               │
│   업로드   │                                               │
│ · 리포트   │                                               │
│   큐       │                                               │
│ · 지식기반 │                                               │
│ · 이슈로그 │                                               │
└───────────┴───────────────────────────────────────────────┘
```

### A-1. 데이터 업로드 화면

**목적:** 고객 기초 자료 업로드 → 정형화 → 확정 수치 계산 트리거.

```
┌── 데이터 업로드 ──────────────────────────────────────────┐
│ 고객 선택 [▼ ○○상사]     기간 [▼ 2025-Q2]                │
│                                                           │
│ ┌───────────────────────────────────────────────────┐   │
│ │   파일을 끌어다 놓거나 클릭하여 업로드              │   │
│ │   (xlsx / csv)                                     │   │
│ └───────────────────────────────────────────────────┘   │
│                                                           │
│ 업로드 미리보기 (파싱 결과 표)                            │
│ ┌───────────────┬──────────┬──────────┐                  │
│ │ 계정과목       │ 금액      │ 전기      │                  │
│ ├───────────────┼──────────┼──────────┤                  │
│ │ 매출액         │ ...      │ ...      │                  │
│ └───────────────┴──────────┴──────────┘                  │
│                                                           │
│                    [수치 확정 계산 실행]  ← compute_*     │
└───────────────────────────────────────────────────────────┘
```

- **컴포넌트:** 고객/기간 셀렉트, 드롭존, 파싱 미리보기 테이블, "수치 확정 계산 실행" 버튼.
- **데이터 바인딩:** 업로드 → `ingest` → 미리보기; 실행 → `compute_pl`/`compute_bs` → `financials.*` 저장 → 리포트 `status=computed`.

### A-2. 리포트 큐 (목록)

```
┌── 리포트 큐 ──────────────────────────────────────────────┐
│ [검토중] [재작성중] [전체]                     검색 [____] │
│ ┌──────────┬────────┬─────────┬───────────┬────────────┐  │
│ │ 고객      │ 기간    │ 버전     │ 상태       │ 업데이트    │  │
│ ├──────────┼────────┼─────────┼───────────┼────────────┤  │
│ │ ○○상사   │ 2025Q2 │ v1      │ 🟡 검토중  │ 2분 전     │  │
│ │ △△유통   │ 2025Q2 │ v2      │ 🟣 재작성  │ 10분 전    │  │
│ └──────────┴────────┴─────────┴───────────┴────────────┘  │
└───────────────────────────────────────────────────────────┘
```

- **데이터 바인딩:** `report_drafts`(status, version, updated_at) 목록. 행 클릭 → A-3 상세.

### A-3. 초안 검토 뷰 (핵심 화면)

**목적:** Agent 1~3 산출물을 확정 수치와 병렬로 검토(D2).

```
┌── ○○상사 · 2025-Q2 · v1 ───────────────  🟡 검토중  ────┐
│ 탭: [ 종합 ] [ PL 분석(A1) ] [ BS 분석(A2) ] [ 모순점검 ] │
├───────────────────────────────┬───────────────────────────┤
│  확정 수치 (원본)              │  에이전트 서술            │
│  ┌─────────────┬──────────┐    │  • 영업이익률 8.0→14.2%   │
│  │ 영업이익률   │ 14.2%    │    │    개선 (source: OPM)     │
│  │ 매출총이익률 │ 35.0%    │    │  • 판관비 레버리지 효과   │
│  │ 유동비율     │ 150.0%   │    │    (source: OPEX, REV)    │
│  │ 부채비율     │ 137.5%   │    │  ...                      │
│  └─────────────┴──────────┘    │                           │
│  (source_ref 클릭 시 좌측 수치  │  각 문장 옆 [🔗 근거]     │
│   하이라이트)                   │                           │
├───────────────────────────────┴───────────────────────────┤
│  ⚠ 모순 플래그: 수익성 개선 vs 부채비율 상승 (미해결)      │
├───────────────────────────────────────────────────────────┤
│  [전문가 피드백 작성 ▼]                    [초안 반려]     │
└───────────────────────────────────────────────────────────┘
```

- **컴포넌트:** 탭(종합/PL/BS/모순), 좌우 2단(확정 수치 표 ↔ 에이전트 서술), `source_ref` 하이라이트 상호작용, 모순 플래그 배너.
- **데이터 바인딩:**
  - 좌측 수치 표 ← `financials.pl.ratios` / `financials.bs.ratios` (D1: 그대로 렌더).
  - PL 탭 ← `agent1_pl_analysis.findings[].text` + `source_ref`.
  - BS 탭 ← `agent2_bs_analysis.findings[].text` + `severity`.
  - 종합 탭 ← `agent3_draft_report.sections[].body_md`.
  - 모순 탭 ← `agent3_draft_report.contradiction_flags[]`.

### A-4. 전문가 피드백 입력

**목적:** 섹션별 지시·우선순위를 입력해 Agent 4로 전송(HITL Step 5).

```
┌── 전문가 피드백 ──────────────────────────────────────────┐
│ 대상 섹션 [▼ 권고 사항]     우선순위 [ 높음 ▼ ]           │
│ 지시 내용                                                  │
│ ┌───────────────────────────────────────────────────┐    │
│ │ 부채비율 관리 방안으로 매출채권 회수기일 단축을     │    │
│ │ 구체적으로 제안할 것.                               │    │
│ └───────────────────────────────────────────────────┘    │
│                                     [+ 지시 추가]          │
│                                                            │
│ 추가된 지시 목록                                           │
│  1. (권고) 매출채권 회수기일 단축 제안 — 높음   [삭제]     │
│  2. (수익성) 표현 쉽게 — 보통                    [삭제]     │
│                                                            │
│ 전체 코멘트 [ 리스크 톤을 과장하지 말 것. ]                │
│                          [피드백 전송 → Agent 4 재작성]    │
└────────────────────────────────────────────────────────────┘
```

- **데이터 바인딩:** 폼 → `expert_feedback.instructions[]`(target_section, directive, priority) + `overall_note`. 전송 시 상태 `review_pending → revising`.

### A-5. 재작성 결과 비교 & 최종 승인

**목적:** 이전 버전 대비 Agent 4 재작성본을 비교하고, 반영 내역을 확인 후 승인.

```
┌── 재작성 비교 · v1 → v2 ──────────────────  🟣 재작성됨  ─┐
│  ┌── 이전(v1) ───────────┐  ┌── 신규(v2) ───────────┐    │
│  │ ...권고 섹션...        │  │ ...회수기일 30일 단축  │    │
│  │                       │  │  시 운전자본 개선...   │    │
│  └───────────────────────┘  └───────────────────────┘    │
│                                                           │
│  반영 내역 (applied_feedback)                             │
│   ✔ 매출채권 회수기일 단축 제안 → actions에 반영          │
│   ✔ 표현 쉽게 → profitability 톤 조정                     │
│                                                           │
│         [추가 피드백(재검토)]        [최종 승인 & 발행]   │
└───────────────────────────────────────────────────────────┘
```

- **데이터 바인딩:** 좌/우 diff ← `report_drafts` v1/v2 `payload_json.sections`. 반영 내역 ← `agent4_final_report.applied_feedback[]`.
- **동작:** "추가 피드백" → `revising→review_pending` 루프. "최종 승인 & 발행" → `approved` → `dashboard_payload` 생성 → `published`.

---

## B. 고객용 프론트엔드 대시보드

발행 완료(`published`)된 컨설팅 리포트를 고객(사장님)에게 보여주는 화면. **모바일 우선(D4).**

### B-0. 반응형 레이아웃 개념

- 모바일: KPI 카드 1열 세로 스택 → 차트 → 리포트 본문(아코디언).
- 데스크톱: KPI 2×2 그리드 + 차트 + 본문 2단.

### B-1. 대시보드 (모바일 목업)

```
┌───────────────────────────┐
│ ○○상사 · 2025년 2분기      │
│ 재무 컨설팅 리포트         │
├───────────────────────────┤
│ ┌───────────┐             │
│ │ 영업이익률 │  14.2%  ▲   │  ← KPI 카드 (delta, trend)
│ │           │  +6.2%p     │
│ └───────────┘             │
│ ┌───────────┐             │
│ │ 유동비율   │  150.0% ―   │
│ └───────────┘             │
│ ┌───────────┐             │
│ │ 부채비율   │ 137.5% ▲⚠   │  ← flag=watch → 주의 뱃지
│ └───────────┘             │
│ ┌───────────┐             │
│ │ 운전자본   │ 3,000만 ▲   │
│ └───────────┘             │
├───────────────────────────┤
│ 영업이익 추이              │
│  ┌───────────────────┐    │
│  │      ╱             │    │  ← line chart (op_trend)
│  │   ╱                │    │
│  └───────────────────┘    │
├───────────────────────────┤
│ ▸ 종합 요약               │  ← 아코디언 (report_sections)
│ ▸ 수익성                  │
│ ▸ 재무 건전성             │
│ ▸ 권고 사항               │
└───────────────────────────┘
```

- **컴포넌트:** 헤더(고객·기간), KPI 카드(값·delta·trend·flag), 차트, 리포트 섹션 아코디언.
- **데이터 바인딩:**
  - 헤더 ← `dashboard_payload.client`.
  - KPI 카드 ← `dashboard_payload.kpis[]` (label, value_pct/value, delta_pct, trend, flag).
  - 차트 ← `dashboard_payload.charts[]` (type, series_ref, periods).
  - 본문 아코디언 ← `dashboard_payload.report_sections[].body_md` (Markdown 렌더).

### B-2. KPI 카드 컴포넌트 규약

- `trend`: `up`=▲(초록), `down`=▼(빨강), `flat`=―(회색). 단, **좋고 나쁨은 지표 성격에 따름**(부채비율 up은 주의). `flag: "watch"` 시 별도 주의 뱃지 표시.
- 값 포맷: `value_pct`는 `%`, `value`(금액)는 통화 축약(만/억). **포맷팅은 표시용이며 원본 수치를 변경하지 않는다(D1).**

---

## C. 컴포넌트 인벤토리 (요약)

| 컴포넌트 | 사용 화면 | 바인딩 소스 |
|----------|-----------|-------------|
| StatusBadge | 백오피스 전역 | `report_drafts.status` |
| UploadDropzone | A-1 | ingest 입력 |
| FinancialsTable | A-3 좌측 | `financials.*.ratios/accounts` |
| FindingList | A-3 우측 | `agent1/2.findings[]` |
| ContradictionBanner | A-3 | `agent3.contradiction_flags[]` |
| FeedbackForm | A-4 | `expert_feedback` |
| DiffView | A-5 | `report_drafts` v(n-1) vs v(n) |
| AppliedFeedbackList | A-5 | `agent4.applied_feedback[]` |
| KpiCard | B-1 | `dashboard_payload.kpis[]` |
| TrendChart | B-1 | `dashboard_payload.charts[]` |
| ReportAccordion | B-1 | `dashboard_payload.report_sections[]` |

---

## D. 접근성·품질 체크

- 색만으로 정보 전달 금지(배지·trend에 아이콘/텍스트 병기).
- 차트는 대체 텍스트/수치 테이블 제공.
- 모바일에서 가로 스크롤 없이 본문이 읽히도록(넓은 표는 개별 스크롤 컨테이너).
- 라이트/다크 테마 모두 대비 확보.
