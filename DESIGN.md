# DESIGN.md — UI/UX 디자인

> **문서 상태:** 개정 v0.3 (전면 개정, 승인 대기)
> **스택:** HTML + TailwindCSS (반응형). 데이터는 `SPEC.md`(v0.3)의 JSON 스키마와 1:1 바인딩.
> **두 개의 UI:** (A) 관리자용 백오피스, (B) 고객용 프론트엔드 대시보드.
>
> **v0.3 개정 요지:** 고객 프로필(CRM) 등록·전문가 Master 업로드 화면 신설,
> 초안 검토 뷰에 프로필 요약·전후 비교(Follow-up)·미시 분석 카드 추가,
> 고객 대시보드 최상단에 **BEP 달성률·일일 타겟 판매수량** 최우선 KPI 배치.

---

## 0. 디자인 원칙

| # | 원칙 | 설명 |
|---|------|------|
| D1 | **수치는 확정값만 표시** | 모든 숫자는 `financials.*` / `dashboard_payload` 확정값 그대로 렌더. 프론트 재계산 금지. |
| D2 | **원본 대비 분석 병렬 표시** | 백오피스는 "확정 수치(원본)"와 "에이전트 서술"을 나란히. |
| D3 | **상태 가시성** | 상태 배지(computed~published, failed) 상시 노출. |
| D4 | **모바일 우선(고객)** | 사장님이 휴대폰으로 보는 것을 전제로 카드 중심 반응형. |
| D5 | **일관 토큰** | Tailwind 유틸리티 토큰, 라이트/다크 대비 확보. |
| D6 | **목표 우선(고객)** | 고객 대시보드의 첫 화면 첫 카드 = "오늘 몇 개 팔면 되는가"(BEP). 진단보다 행동 목표가 먼저 보인다. |
| D7 | **맥락 우선(관리자)** | 검토 화면 어디서든 고객 프로필(업종·상권·성향)과 전회차 이행 성과가 한눈에 보인다. |

**상태 배지 색상 규약(유지):** `computed`=slate, `drafting`=blue, `review_pending`=amber, `revising`=violet, `approved`=green, `published`=emerald, `failed`=red.

---

## A. 관리자용 백오피스

### A-0. 전역 레이아웃 (사이드바에 '고객(CRM)' 추가)

```
┌───────────────────────────────────────────────────────────┐
│  [로고] 재무컨설팅 백오피스            [사용자]  [로그아웃]  │
├───────────┬───────────────────────────────────────────────┤
│ 사이드바   │  메인 컨텐츠 영역                              │
│ · 대시보드 │                                               │
│ · 고객(CRM)│  ← 신규: 프로필 등록/조회                      │
│ · 데이터   │                                               │
│   업로드   │                                               │
│ · 리포트 큐│                                               │
│ · 지식기반 │                                               │
│ · 이슈로그 │                                               │
└───────────┴───────────────────────────────────────────────┘
```

### A-0.5 고객 프로필 등록 화면 (CRM — 신규)

**목적:** 신규 고객 등록 시 **필수 프로필**을 입력받아 `clients`(+profile)에 저장.
이 프로필이 에이전트 맞춤 분석·RAG 코호트 검색의 기준이 된다(`SPEC §2.6 client_profile`).

```
┌── 고객 등록 (CRM) ────────────────────────────────────────┐
│ 상호(trade_name)*      [ ○○치킨 역삼점        ]           │
│ 대표자명(name)*        [ 김□□                 ]           │
│ 업종(industry)*        [▼ 치킨전문점           ]           │
│ 상권 유형*             [▼ 오피스│주거│유동│대학가│관광│기타] │
│ 정확 위치(내부용)      [ 서울 강남구 역삼동     ]  🔒내부   │
│ 대표자 성별*           ( )남  (•)여                        │
│ 대표자 연령*           [ 34 ]세  → 자동 밴드: 30대 초중반   │
│ 리스크 수용 성향*      [▼ 보수적│중립│공격적    ]           │
│ 메모                   [                        ]           │
│                                    [등록]  [취소]           │
│ ⚠ 정확 위치·나이·상호는 내부 전용입니다. 지식기반(RAG)에는  │
│   상권/연령대 밴드만 저장됩니다.                            │
└────────────────────────────────────────────────────────────┘
```

- **바인딩:** 폼 → `client_profile`. `owner_age` 입력 시 `owner_age_band` 자동 파생 표시.
- 프로필 목록/상세 화면에서 회차 이력(컨설팅 라운드)과 함께 조회.

### A-1. 데이터 업로드 화면 (전문가 가공 Master 업로드 — 개정)

**목적:** 고객 원시 데이터(POS·통장내역)는 **수령함**에 보관만 하고, 전문가가 교차 대조·정제한
**표준화 Master 엑셀/CSV**만 파이프라인에 투입(P5 게이트).

```
┌── 데이터 업로드 ──────────────────────────────────────────┐
│ 고객 [▼ ○○치킨 역삼점]   기간 [▼ 2025-Q3]                 │
│                                                            │
│ ① 원시 데이터 수령함 (참고용 보관 — 파이프라인 미투입)      │
│   · POS_2025Q3.xlsx  · 통장내역_2025Q3.csv   [+ 추가]      │
│                                                            │
│ ② 전문가 정제 Master 업로드 (파이프라인 투입)               │
│ ┌───────────────────────────────────────────────────┐    │
│ │  표준화 Master 엑셀/CSV 를 끌어다 놓으세요          │    │
│ │  (양식: sales_details / cogs_details / opex_details │    │
│ │   / inventory / debt / owner_draws 시트)            │    │
│ └───────────────────────────────────────────────────┘    │
│                                                            │
│ 파싱 미리보기 + 정합성 체크                                 │
│  ✔ 품목 매출 합계 vs 총매출 대사: 차액 9,100,000원          │
│    → unallocated_cash_sales 로 분류됩니다                   │
│  ✖ 판매수량 0인 품목 2건 → 확인 필요                        │
│                                                            │
│                    [수치 확정 계산 실행]  ← compute_*       │
└────────────────────────────────────────────────────────────┘
```

- **바인딩:** ② 업로드 → `ingest` → `compute_pl`/`compute_bs`. 대사 차액은
  `unallocated_cash_sales`로, 불일치·수량 0은 업로드 단계에서 경고(`reconciliation_error`,
  `division_by_zero` 예방).

### A-2. 리포트 큐 — 유지 (회차(period)·버전·상태·업데이트)

### A-3. 초안 검토 뷰 (핵심 화면 — 개정)

**목적:** 프로필·전회차 맥락(D7) 위에서, 확정 수치와 에이전트 서술을 병렬 검토(D2).

```
┌── ○○치킨 역삼점 · 2025-Q3 · v1 ──────────  🟡 검토중  ────┐
│ ┌ 사업장 프로필 요약 ───────────────────────────────────┐ │
│ │ 치킨전문점 · 오피스 상권 · 대표 여/30대 초중반 · 보수적 │ │
│ └───────────────────────────────────────────────────────┘ │
│ ┌ 전회차(2025-Q2) 권고 이행 추적 ───────────────────────┐ │
│ │ R-01 회수기일 단축  42→33일  ✔ 개선                    │ │
│ │ R-02 부채비율 관리  137.5→122.1%  ✔ 개선               │ │
│ └───────────────────────────────────────────────────────┘ │
│ 탭: [ 종합 ] [ PL(A1) ] [ BS(A2) ] [ 모순점검 ]            │
├───────────────────────────────┬────────────────────────────┤
│  확정 수치 (원본)              │  에이전트 서술              │
│  --- PL 탭 ---                │  · BEP 일일 목표: 38개/일   │
│  효자/부진 품목 TOP3           │    (bep_guidance)           │
│   1 후라이드 60.0% ▲          │  · 생닭 +24.2% × 마진 압박  │
│   3 콜라(병) 30.0% ▼          │    교차 경고                │
│  원부자재/판관비 낭비 타겟      │  · 현금 매출 7.0% 미매칭    │
│   생닭 +24.2% · 배달수수료 +20.6%│   → 세무 리스크(warn)     │
│  현금 매출 비중 경고 배너 ⚠     │                            │
│  --- BS 탭 ---                │  · 가수금 12,000,000원 혼용 │
│  Cash Runway 2.7개월 ⚠        │    리스크(high)             │
│  DSCR 2.1 · CCC 16일          │  · 부가세 1.2개월 후 도래 → │
│  부채 금리/상환 압박도          │    Cash Reserve 권고(high)  │
│   ○○은행 5.2% 185만/월        │                            │
│   △△캐피탈 9.8% 123만/월 ⚠    │                            │
│  가수금 투입 현황 12,000,000원  │                            │
├───────────────────────────────┴────────────────────────────┤
│  ⚠ 모순 플래그: ...                                         │
│  [전문가 피드백 작성 ▼]                     [초안 반려]      │
└─────────────────────────────────────────────────────────────┘
```

- **바인딩:**
  - 프로필 요약 ← `client_profile`. 이행 추적 ← `followup_context`(previous_recommendations + metric_progress).
  - PL 탭: 효자/부진 TOP3 ← `sales_details[].margin_rank/margin_pct`; 낭비 타겟 ←
    `cogs_details[].yoy_pct` 상위 + `opex_details[].increase_rank`; 경고 배너 ←
    `unallocated_cash_sales.risk_flag`; BEP ← `bep.*` + `agent1.bep_guidance`.
  - BS 탭: Cash Runway/DSCR/CCC ← `cash_flow.*`; 상환 압박 ← `debt_details[]`(금리·월상환 정렬);
    가수금 ← `owner_draws.*`; 세무 경고 ← `upcoming_tax_events` + `agent2.cash_reserve_alerts`.

### A-4. 전문가 피드백 입력 — 유지

### A-5. 재작성 비교 & 최종 승인 — 유지 + **승인 시 `recommendations[]` 미리보기**(발행되면 다음 회차 이행 추적 기준이 됨을 안내)

---

## B. 고객용 프론트엔드 대시보드

### B-1. 대시보드 (모바일 목업 — 개정)

**최상단 최우선(Hero) KPI = BEP 달성률 + 일일 타겟 판매수량**(D6). 사장님이 앱을 열자마자
"오늘 몇 개 팔면 되는지"를 인식한다.

```
┌───────────────────────────┐
│ ○○치킨 역삼점 · 2025년 3분기│
├───────────────────────────┤
│ ★ HERO KPI                │
│ ┌───────────────────────┐ │
│ │ 손익분기점 달성률       │ │
│ │      248.4%  ✔ 안전    │ │  ← bep_attainment_pct
│ │ ▓▓▓▓▓▓▓▓▓▓░ (게이지)   │ │
│ └───────────────────────┘ │
│ ┌───────────────────────┐ │
│ │ 오늘의 목표             │ │
│ │ 후라이드 38개/일 판매 시 │ │  ← daily_target_qty
│ │ 손익분기 달성            │ │
│ └───────────────────────┘ │
├───────────────────────────┤
│ 지난 분기 약속, 이렇게 됐어요│  ← Follow-up 전후 비교
│  회수기일 42→33일  ✔ 개선   │
│  부채비율 137.5→122.1% ✔    │
├───────────────────────────┤
│ ┌─────────┐ ┌─────────┐   │
│ │영업이익률│ │현금 생존 │   │
│ │ 14.2% ― │ │ 2.7개월⚠│   │
│ └─────────┘ └─────────┘   │
│ ┌─────────┐ ┌─────────┐   │
│ │DSCR 2.1 │ │CCC 16일 │   │
│ └─────────┘ └─────────┘   │
├───────────────────────────┤
│ 영업이익 추이 (차트)        │
├───────────────────────────┤
│ ▸ 종합 요약                │
│ ▸ 우리 가게 효자/아픈 메뉴  │
│ ▸ 현금흐름·세금 대비        │
│ ▸ 권고 사항                │
└───────────────────────────┘
```

- **바인딩:**
  - Hero ← `dashboard_payload.hero_kpis[]`(bep_attainment / daily_target_qty). 게이지는 표시용 시각화일 뿐 수치 재계산 없음(D1).
  - 전후 비교 ← `dashboard_payload.followup.items[]`.
  - 일반 KPI ← `kpis[]`(OPM·cash_runway·dscr·ccc — flag=watch 시 ⚠).
  - 본문 아코디언 ← `report_sections[]`.

### B-2. KPI 카드 컴포넌트 규약 — 유지 + 추가

- Hero KPI는 일반 KPI보다 시각 위계 2단계 상위(전폭 카드·큰 타이포).
- `cash_runway` 등 "낮을수록 위험" 지표는 임계 flag(`watch`/`critical`)에 따라 색·아이콘 구분.
- 포맷팅은 표시용, 원본 수치 불변(D1).

---

## C. 컴포넌트 인벤토리 (개정)

| 컴포넌트 | 사용 화면 | 바인딩 소스 |
|----------|-----------|-------------|
| StatusBadge | 백오피스 전역 | `report_drafts.status` |
| ClientProfileForm **(신규)** | A-0.5 | `client_profile` |
| ProfileSummaryBar **(신규)** | A-3, B-1 | `client_profile`(요약 표기) |
| RawInboxList **(신규)** | A-1① | 원시 데이터 수령함(참고 보관) |
| MasterUploadDropzone **(개정)** | A-1② | Master 양식 → ingest |
| ReconciliationPreview **(신규)** | A-1 | 대사 차액·수량0 경고 |
| FollowupTracker **(신규)** | A-3, B-1 | `followup_context` / `dashboard_payload.followup` |
| ItemMarginTop3 **(신규)** | A-3 PL | `sales_details[].margin_rank` |
| WasteTargetList **(신규)** | A-3 PL | `cogs_details`·`opex_details[].increase_rank` |
| CashSalesWarningBanner **(신규)** | A-3 PL | `unallocated_cash_sales.risk_flag` |
| BepHeroCard **(신규)** | B-1 최상단 | `hero_kpis`(bep_attainment, daily_target_qty) |
| CashRunwayCard **(신규)** | A-3 BS, B-1 | `cash_flow.cash_runway_months` |
| DebtPressureTable **(신규)** | A-3 BS | `debt_details[]`(금리·월상환) |
| OwnerDrawsCard **(신규)** | A-3 BS | `owner_draws.*` |
| TaxReserveAlert **(신규)** | A-3 BS, B-1 | `upcoming_tax_events` + `cash_reserve_alerts` |
| FinancialsTable / FindingList / ContradictionBanner / FeedbackForm / DiffView / AppliedFeedbackList | 기존 유지 | 기존 바인딩 |
| RecommendationsPreview **(신규)** | A-5 | `recommendations[]` |
| KpiCard / TrendChart / ReportAccordion | B-1 | 기존 바인딩 |

---

## D. 접근성·품질 체크 — 유지

- 색만으로 정보 전달 금지, 차트 대체 텍스트, 모바일 가로 스크롤 금지, 라이트/다크 대비.
- **추가:** Hero KPI(BEP)는 숫자+문장 병기("하루 38개") — 수치 문해력이 낮은 사용자도 즉시 이해.
